import React, { Component } from "react";

class Prefs extends Component {
    render() {
        return (
            <div>Choose default display query prefs</div>
        )
    }
}

export default Prefs;